module {
  /// abs0
  ///
  /// Native function
  ///
  /// abs0 is a native function used to generate abs and sine functions
  public func abs0(x : Float) : Float {
    let y : Float = 0;
    if (x >= y) {
      return x;
    } else {
      return -x;
    };
  };
};
